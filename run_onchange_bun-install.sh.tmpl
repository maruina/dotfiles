{{- if eq .profile "work" -}}
#!/bin/bash
set -eufo pipefail

if ! command -v bun >/dev/null 2>&1; then
  echo "bun is required to install mcp-cli" >&2
  exit 1
fi

bun install -g https://github.com/philschmid/mcp-cli
{{- end }}
