alias ..="cd .."
alias ...="cd ../.."
alias ....="cd ../../.."

alias kap="docker run -t --rm -v $(pwd):/src:delegated deepmind/kapitan"

alias docker-rm-stopped="docker rm $(docker ps -a -q)"
alias docker-rm-images="docker rmi $(docker images -a -q)"
